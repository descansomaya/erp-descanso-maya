/**
 * DESCANSO MAYA ERP - Motor Principal v41 (VERSIÓN DEFINITIVA)
 * Fase 1 (Buscadores, Semáforos, Filtro Mes) + Fase 2 (Alertas Stock, Mayoreo)
 */

window.onerror = function(message, source, lineno) { alert("Hubo un error al cargar: \n" + message + "\nLínea: " + lineno); };

window.cargarTarifas = function(artesanoId) { const tarifas = App.state.tarifas_artesano.filter(t => t.artesano_id === artesanoId); const select = document.getElementById('select-tarifas'); if(!select) return; select.innerHTML = '<option value="">-- Seleccione Trabajo --</option>' + tarifas.map(t => `<option value="${t.monto}">${t.clasificacion} ($${t.monto})</option>`).join(''); };
window.calcTotalTrabajo = function() { const sel = document.getElementById('select-tarifas'); const cant = document.getElementById('cant-trabajo').value; const tot = document.getElementById('total-trabajo'); if(sel && cant && tot && sel.value) { tot.value = (parseFloat(sel.value) * parseFloat(cant)).toFixed(2); document.getElementById('tarea_nombre').value = sel.options[sel.selectedIndex].text.split(' ($')[0]; } };
window.filtrarLista = function(inputId, claseItem) { const filtro = document.getElementById(inputId).value.toLowerCase(); const items = document.querySelectorAll('.' + claseItem); items.forEach(item => { const texto = item.innerText.toLowerCase(); item.style.display = texto.includes(filtro) ? '' : 'none'; }); };

window.calcularTotalPedido = function() {
    const prodSelect = document.querySelector('select[name="producto_id"]'); const cantInput = document.querySelector('input[name="cantidad"]'); const mayoreoCheck = document.querySelector('input[name="es_mayoreo"]'); const totalInput = document.querySelector('input[name="total"]');
    if(prodSelect && cantInput && totalInput && prodSelect.value) {
        const prod = App.state.productos.find(p => p.id === prodSelect.value);
        if(prod) { const cant = parseFloat(cantInput.value) || 1; const usaMayoreo = mayoreoCheck && mayoreoCheck.checked; const precioBase = usaMayoreo && parseFloat(prod.precio_mayoreo) > 0 ? parseFloat(prod.precio_mayoreo) : parseFloat(prod.precio_venta); totalInput.value = (precioBase * cant).toFixed(2); }
    }
};

const App = {
    state: {
        config: { 
            empresa: "Descanso Maya", 
            moneda: "MXN",
            logoUrl: "https://i.imgur.com/kK5R3v4.png", // <-- LÍNEA 26: LOGO
            redesSociales: "@descansomaya.mx"           // <-- LÍNEA 27: REDES
        },
        pinAcceso: localStorage.getItem('erp_pin') || null, 
        cotizaciones: JSON.parse(localStorage.getItem('erp_cotizaciones')) || [], 
        pedidos: [], pedido_detalle: [], ordenes_produccion: [], artesanos: [], 
        inventario: [], productos: [], clientes: [], abonos: [], gastos: [], 
        compras: [], proveedores: [], reparaciones: [], tarifas_artesano: [], pago_artesanos: [], consumos_produccion: []
    },

    api: {
        gasUrl: "https://script.google.com/macros/s/AKfycbxL3KzjesyZIfiC-Dyr0SwwzwNnPsv5FgHpt-JhyscNpN1eTvRwAh_rdgoxdVnKTAwu/exec", // <-- LÍNEA 37: ¡URL DE GOOGLE SCRIPT!
        async fetch(action, payload = {}) {
            try { const response = await fetch(this.gasUrl, { method: 'POST', body: JSON.stringify({ action: action, payload: payload, pin: App.state.pinAcceso }) }); const data = await response.json(); if (data.message === "Acceso Denegado. PIN incorrecto.") { localStorage.removeItem('erp_pin'); App.state.pinAcceso = null; App.router.handleRoute(); } return data; } 
            catch (error) { return { status: "error", message: "Fallo de conexión." }; }
        }
    },

    ui: {
        container: null,
        init() { this.container = document.getElementById('overlays'); const toastCont = document.createElement('div'); toastCont.className = 'toast-container'; toastCont.id = 'toast-container'; document.body.appendChild(toastCont); const loader = document.createElement('div'); loader.id = 'global-loader'; loader.innerHTML = `<div class="spinner"></div><h2 style="margin: 0; font-weight: 600;">Descanso Maya</h2><p id="loader-text" style="margin-top: 10px; opacity: 0.8; font-size: 0.9rem;">Sincronizando módulos...</p><button id="btn-reintentar" class="btn btn-secondary hidden" style="margin-top: 20px;" onclick="location.reload()">Reintentar</button>`; document.body.appendChild(loader); },
        toast(message) { const cont = document.getElementById('toast-container'); const t = document.createElement('div'); t.className = 'toast'; t.textContent = message; cont.appendChild(t); setTimeout(() => t.remove(), 4000); },
        showLoader(mensaje = "Procesando...") { const loader = document.getElementById('global-loader'); if(loader) { document.getElementById('loader-text').textContent = mensaje; loader.classList.remove('hidden'); } },
        hideLoader() { const loader = document.getElementById('global-loader'); if(loader) loader.classList.add('hidden'); },
        openSheet(title, contentHTML, onSaveCallback) { this.container.innerHTML = `<div class="overlay-bg active" id="sheet-bg"></div><div class="bottom-sheet active" id="sheet-content"><div class="sheet-header"><h3>${title}</h3><button class="sheet-close" id="sheet-close">&times;</button></div><div class="sheet-body">${contentHTML}</div></div>`; document.getElementById('sheet-close').onclick = this.closeSheet.bind(this); document.getElementById('sheet-bg').onclick = this.closeSheet.bind(this); const form = document.getElementById('dynamic-form'); if(form && onSaveCallback) { form.onsubmit = (e) => { e.preventDefault(); const data = Object.fromEntries(new FormData(form).entries()); this.closeSheet(); onSaveCallback(data); }; } },
        closeSheet() { this.container.innerHTML = ''; }
    },

    logic: {
        async verificarPIN(pinIngresado) { App.ui.showLoader("Verificando..."); App.state.pinAcceso = pinIngresado; const res = await App.api.fetch("ping"); if (res.status === "success") { localStorage.setItem('erp_pin', pinIngresado); App.ui.toast("¡Acceso concedido!"); this.cargarDatosIniciales(); } else { App.state.pinAcceso = null; App.ui.hideLoader(); App.ui.toast("PIN Incorrecto."); } },
        async cargarDatosIniciales() { App.ui.showLoader("Descargando bases de datos..."); try { const hojas = ["materiales", "clientes", "productos", "pedidos", "pedido_detalle", "ordenes_produccion", "artesanos", "abonos_clientes", "gastos", "compras", "proveedores", "reparaciones", "tarifas_artesano", "pago_artesanos", "consumos_produccion"]; const promesas = hojas.map(h => App.api.fetch("leer_hoja", { nombreHoja: h })); const resultados = await Promise.all(promesas); if (resultados[0].status === "error") throw new Error(resultados[0].message); App.state.inventario = resultados[0].data || []; App.state.clientes = resultados[1].data || []; App.state.productos = resultados[2].data || []; App.state.pedidos = resultados[3].data || []; App.state.pedido_detalle = resultados[4].data || []; App.state.ordenes_produccion = resultados[5].data || []; App.state.artesanos = resultados[6].data || []; App.state.abonos = resultados[7].data || []; App.state.gastos = resultados[8].data || []; App.state.compras = resultados[9].data || []; App.state.proveedores = resultados[10].data || []; App.state.reparaciones = resultados[11].data || []; App.state.tarifas_artesano = resultados[12].data || []; App.state.pago_artesanos = resultados[13].data || []; App.state.consumos_produccion = resultados[14].data || []; App.ui.hideLoader(); App.router.init(); } catch (error) { App.ui.toast("Error de red."); document.getElementById('btn-reintentar').classList.remove('hidden'); } },
        descargarRespaldo() { const dataStr = "data:text/json;charset=utf-8," + encodeURIComponent(JSON.stringify(App.state, null, 2)); const dlAnchorElem = document.createElement('a'); dlAnchorElem.setAttribute("href", dataStr); dlAnchorElem.setAttribute("download", `Respaldo_ERP_Maya_${new Date().toISOString().split('T')[0]}.json`); dlAnchorElem.click(); App.ui.toast("Respaldo descargado"); },

        async eliminarRegistroGenerico(nombreHoja, idFila, nombreEstadoApp) { if(!confirm("⚠️ ¿Eliminar permanentemente?")) return; App.ui.showLoader("Eliminando..."); const res = await App.api.fetch("eliminar_fila", { nombreHoja: nombreHoja, idFila: idFila }); App.ui.hideLoader(); if(res.status === "success") { App.state[nombreEstadoApp] = App.state[nombreEstadoApp].filter(item => item.id !== idFila); App.ui.toast("Eliminado"); App.router.handleRoute(); } else { App.ui.toast("Error"); } },
        async eliminarPedido(pedidoId) { if(!confirm("⚠️ ¿Eliminar este pedido? Se regresará el material al inventario.")) return; App.ui.showLoader("Restaurando inventario..."); const pedido = App.state.pedidos.find(p => p.id === pedidoId); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === pedidoId); const orden = App.state.ordenes_produccion.find(o => detalle && o.pedido_detalle_id === detalle.id); if(detalle) { const producto = App.state.productos.find(p => p.id === detalle.producto_id); if(producto && (!orden || orden.estado !== 'listo')) { for(let i=1; i<=5; i++) { const matId = producto[`mat_${i}`]; const cant = parseFloat(producto[`cant_${i}`] || 0); if(matId && cant > 0) { const material = App.state.inventario.find(m => m.id === matId); if(material) { const nuevoStock = parseFloat(material.stock_actual) + (cant * parseInt(detalle.cantidad || 1)); await App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: material.id, datosNuevos: { stock_actual: nuevoStock } }); material.stock_actual = nuevoStock; } } } } } await App.api.fetch("eliminar_fila", { nombreHoja: "pedidos", idFila: pedidoId }); if(detalle) await App.api.fetch("eliminar_fila", { nombreHoja: "pedido_detalle", idFila: detalle.id }); if(orden) await App.api.fetch("eliminar_fila", { nombreHoja: "ordenes_produccion", idFila: orden.id }); App.state.pedidos = App.state.pedidos.filter(p => p.id !== pedidoId); if(detalle) App.state.pedido_detalle = App.state.pedido_detalle.filter(d => d.id !== detalle.id); if(orden) App.state.ordenes_produccion = App.state.ordenes_produccion.filter(o => o.id !== orden.id); App.ui.hideLoader(); App.ui.toast("Eliminado"); App.router.handleRoute(); },
        async eliminarCompra(compraId) { if(!confirm("⚠️ ¿Eliminar compra? Ajusta manualmente tu inventario.")) return; App.ui.showLoader("Eliminando..."); const res = await App.api.fetch("eliminar_fila", { nombreHoja: "compras", idFila: compraId }); App.ui.hideLoader(); if(res.status === "success") { App.state.compras = App.state.compras.filter(c => c.id !== compraId); App.ui.toast("Compra eliminada."); App.router.handleRoute(); } else { App.ui.toast("Error"); } },

        async actualizarRegistroGenerico(nombreHoja, idFila, datosNuevos, nombreEstadoApp) { App.ui.showLoader("Guardando..."); const res = await App.api.fetch("actualizar_fila", { nombreHoja: nombreHoja, idFila: idFila, datosNuevos: datosNuevos }); App.ui.hideLoader(); if (res.status === "success") { const index = App.state[nombreEstadoApp].findIndex(item => item.id === idFila); if (index !== -1) App.state[nombreEstadoApp][index] = { ...App.state[nombreEstadoApp][index], ...datosNuevos }; App.ui.toast("Actualizado"); App.router.handleRoute(); } else { App.ui.toast("Error"); } },
        async guardarNuevoGenerico(nombreHoja, datos, prefijoID, nombreEstadoApp) { App.ui.showLoader("Registrando..."); datos.id = prefijoID + "-" + Date.now(); if(!datos.fecha_creacion) datos.fecha_creacion = new Date().toISOString(); const res = await App.api.fetch("guardar_fila", { nombreHoja: nombreHoja, datos: datos }); App.ui.hideLoader(); if (res.status === "success") { App.state[nombreEstadoApp].push(datos); App.ui.toast("Guardado"); App.router.handleRoute(); } else { App.ui.toast("Error"); } },

        guardarCotizacion(datos) { datos.id = "COT-" + Date.now(); datos.fecha_creacion = new Date().toISOString(); App.state.cotizaciones.push(datos); localStorage.setItem('erp_cotizaciones', JSON.stringify(App.state.cotizaciones)); App.ui.toast("Cotización generada"); App.router.handleRoute(); App.logic.imprimirCotizacion(datos.id); },
        eliminarCotizacion(id) { if(!confirm("⚠️ ¿Eliminar cotización?")) return; App.state.cotizaciones = App.state.cotizaciones.filter(c => c.id !== id); localStorage.setItem('erp_cotizaciones', JSON.stringify(App.state.cotizaciones)); App.ui.toast("Eliminada"); App.router.handleRoute(); },

        async guardarOrdenStock(datosFormulario) { App.ui.showLoader("Procesando..."); const pedidoId = "PED-STOCK-" + Date.now(); const cantidadNum = parseInt(datosFormulario.cantidad) || 1; const datosPedido = { id: pedidoId, cliente_id: "STOCK_INTERNO", estado: "nuevo", total: 0, anticipo: 0, notas: "Producción Interna", fecha_entrega: "", fecha_creacion: new Date().toISOString() }; const datosDetalle = { id: "PDET-" + Date.now(), pedido_id: pedidoId, producto_id: datosFormulario.producto_id, cantidad: cantidadNum, precio_unitario: 0 }; const producto = App.state.productos.find(p => p.id === datosFormulario.producto_id); const promesaMateriales = []; if(producto) { for(let i=1; i<=5; i++) { const matId = producto[`mat_${i}`]; const cantTeorica = parseFloat(producto[`cant_${i}`] || 0); if(matId && cantTeorica > 0) { const material = App.state.inventario.find(m => m.id === matId); if(material) { const nuevoStock = parseFloat(material.stock_actual) - (cantTeorica * cantidadNum); promesaMateriales.push( App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: material.id, datosNuevos: { stock_actual: nuevoStock } }).then(()=> material.stock_actual = nuevoStock) ); } } } } const resPedido = await App.api.fetch("guardar_fila", { nombreHoja: "pedidos", datos: datosPedido }); if (resPedido.status === "success") { await App.api.fetch("guardar_fila", { nombreHoja: "pedido_detalle", datos: datosDetalle }); const nuevaOrden = { id: "ORD-" + Date.now(), pedido_detalle_id: datosDetalle.id, estado: "pendiente", fecha_creacion: new Date().toISOString() }; await App.api.fetch("guardar_fila", { nombreHoja: "ordenes_produccion", datos: nuevaOrden }); await Promise.all(promesaMateriales); App.state.pedidos.push(datosPedido); App.state.pedido_detalle.push(datosDetalle); App.state.ordenes_produccion.push(nuevaOrden); App.ui.hideLoader(); App.ui.toast("Orden creada"); App.router.handleRoute(); } else { App.ui.hideLoader(); App.ui.toast("Error"); } },
        async guardarNuevoPedido(datosFormulario) { App.ui.showLoader("Procesando pedido..."); const pedidoId = "PED-" + Date.now(); const totalNum = parseFloat(datosFormulario.total) || 0; const anticipoNum = parseFloat(datosFormulario.anticipo) || 0; const cantidadNum = parseInt(datosFormulario.cantidad) || 1; const datosPedido = { id: pedidoId, cliente_id: datosFormulario.cliente_id, estado: "nuevo", total: totalNum, anticipo: anticipoNum, notas: datosFormulario.notas, fecha_entrega: datosFormulario.fecha_entrega, fecha_creacion: new Date().toISOString() }; const datosDetalle = { id: "PDET-" + Date.now(), pedido_id: pedidoId, producto_id: datosFormulario.producto_id, cantidad: cantidadNum, precio_unitario: totalNum / cantidadNum }; const producto = App.state.productos.find(p => p.id === datosFormulario.producto_id); const promesaMateriales = []; if(producto) { for(let i=1; i<=5; i++) { const matId = producto[`mat_${i}`]; const cantTeorica = parseFloat(producto[`cant_${i}`] || 0); if(matId && cantTeorica > 0) { const material = App.state.inventario.find(m => m.id === matId); if(material) { const nuevoStock = parseFloat(material.stock_actual) - (cantTeorica * cantidadNum); promesaMateriales.push( App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: material.id, datosNuevos: { stock_actual: nuevoStock } }).then(()=> material.stock_actual = nuevoStock) ); } } } } const resPedido = await App.api.fetch("guardar_fila", { nombreHoja: "pedidos", datos: datosPedido }); if (resPedido.status === "success") { await App.api.fetch("guardar_fila", { nombreHoja: "pedido_detalle", datos: datosDetalle }); await Promise.all(promesaMateriales); App.state.pedidos.push(datosPedido); App.state.pedido_detalle.push(datosDetalle); App.ui.hideLoader(); App.ui.toast("Pedido guardado"); App.router.handleRoute(); } else { App.ui.hideLoader(); App.ui.toast("Error"); } },
        async mandarAProduccion(pedidoId) { App.ui.showLoader("Generando..."); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === pedidoId); if (!detalle) return; const nuevaOrden = { id: "ORD-" + Date.now(), pedido_detalle_id: detalle.id, estado: "pendiente", fecha_creacion: new Date().toISOString() }; const res = await App.api.fetch("guardar_fila", { nombreHoja: "ordenes_produccion", datos: nuevaOrden }); App.ui.hideLoader(); if (res.status === "success") { App.state.ordenes_produccion.push(nuevaOrden); App.ui.toast("Enviado a taller"); App.router.navigate('produccion'); } },
        async guardarTrabajoOrden(ordenId, data, esReparacion = false) { App.ui.showLoader("Asignando..."); const sanitizedTaskName = (data.tarea_nombre || "Trabajo").replace(/[^a-zA-Z0-9_ \-]/g, ""); const pagoObj = { id: "PAGO-" + Date.now() + "-" + sanitizedTaskName, artesano_id: data.artesano_id, orden_id: ordenId, monto_unitario: parseFloat(data.tarea_val), total: parseFloat(data.total), estado: "pendiente", fecha: new Date().toISOString() }; const res = await App.api.fetch("guardar_fila", { nombreHoja: "pago_artesanos", datos: pagoObj }); App.ui.hideLoader(); if(res.status === "success") { App.state.pago_artesanos.push(pagoObj); App.ui.toast("Asignado"); if(esReparacion) { App.router.handleRoute(); } else { App.views.modalEditarOrden(ordenId); } } else { App.ui.toast("Error"); App.router.handleRoute(); } },
        async cerrarOrdenProduccion(datosFormulario) { App.ui.showLoader("Finalizando..."); const ordenId = datosFormulario.orden_id; const orden = App.state.ordenes_produccion.find(o => o.id === ordenId); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === orden.pedido_detalle_id); const producto = App.state.productos.find(p => p.id === detalle.producto_id); await App.api.fetch("actualizar_fila", { nombreHoja: "ordenes_produccion", idFila: ordenId, datosNuevos: { estado: 'listo' } }); orden.estado = 'listo'; for(let i=1; i<=5; i++) { const matId = datosFormulario[`mat_${i}_id`]; const consumoTeorico = parseFloat(datosFormulario[`mat_${i}_teorico`]); const consumoReal = parseFloat(datosFormulario[`mat_${i}_real`]); if(matId && !isNaN(consumoTeorico) && !isNaN(consumoReal)) { const material = App.state.inventario.find(m => m.id === matId); if(material) { const diferencia = consumoReal - consumoTeorico; if(diferencia !== 0) { const stockAjustado = parseFloat(material.stock_actual) - diferencia; await App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: material.id, datosNuevos: { stock_actual: stockAjustado } }); material.stock_actual = stockAjustado; } const consumoObj = { id: "CONS-" + Date.now() + i, orden_id: ordenId, material_id: material.id, cantidad_teorica: consumoTeorico, cantidad_real: consumoReal, fecha: new Date().toISOString() }; await App.api.fetch("guardar_fila", { nombreHoja: "consumos_produccion", datos: consumoObj }); App.state.consumos_produccion.push(consumoObj); } } } if (datosFormulario.sumar_stock === "1" && producto) { let matHamaca = App.state.inventario.find(m => m.nombre === producto.nombre && m.tipo === 'reventa'); if(matHamaca) { let nStock = parseFloat(matHamaca.stock_actual) + 1; await App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: matHamaca.id, datosNuevos: { stock_actual: nStock } }); matHamaca.stock_actual = nStock; } else { let nMat = { id: "MAT-" + Date.now() + "REV", nombre: producto.nombre, tipo: "reventa", unidad: "Pzas", stock_actual: 1, fecha_creacion: new Date().toISOString() }; await App.api.fetch("guardar_fila", { nombreHoja: "materiales", datos: nMat }); App.state.inventario.push(nMat); } App.ui.toast("Guardado en Bodega"); } App.ui.hideLoader(); App.ui.toast("¡Terminado!"); App.router.handleRoute(); },
        async procesarCambioOrden(ordenId, datos) { if (datos.estado === 'listo') { App.views.formCerrarOrden(ordenId); } else { App.ui.showLoader("Actualizando..."); const res = await App.api.fetch("actualizar_fila", { nombreHoja: "ordenes_produccion", idFila: ordenId, datosNuevos: datos }); App.ui.hideLoader(); if (res.status === "success") { const ordenIndex = App.state.ordenes_produccion.findIndex(o => o.id === ordenId); App.state.ordenes_produccion[ordenIndex] = { ...App.state.ordenes_produccion[ordenIndex], ...datos }; App.ui.toast("Actualizado"); App.router.handleRoute(); } else { App.ui.toast("Error"); } } },
        async liquidarNomina(artesanoId) { App.ui.showLoader("Liquidando..."); const pagosPendientes = App.state.pago_artesanos.filter(p => p.artesano_id === artesanoId && p.estado === 'pendiente'); let totalPagado = 0; const promesasDePago = []; for (let pago of pagosPendientes) { promesasDePago.push( App.api.fetch("actualizar_fila", { nombreHoja: "pago_artesanos", idFila: pago.id, datosNuevos: { estado: 'pagado' } }).then(res => { if(res.status === "success") { pago.estado = 'pagado'; totalPagado += parseFloat(pago.total || 0); } }) ); } await Promise.all(promesasDePago); if (totalPagado > 0) { const artesano = App.state.artesanos.find(a => a.id === artesanoId); const gastoObj = { id: "GAS-" + Date.now(), descripcion: "Nómina - " + (artesano ? artesano.nombre : 'Artesano'), categoria: "nomina", monto: totalPagado, fecha: new Date().toISOString().split('T')[0] }; await App.api.fetch("guardar_fila", { nombreHoja: "gastos", datos: gastoObj }); App.state.gastos.push(gastoObj); } App.ui.hideLoader(); App.ui.toast(`Liquidados $${totalPagado}`); App.router.handleRoute(); },
        
        async guardarNuevaCompra(datos) { App.ui.showLoader("Comprando..."); const compraId = "COM-" + Date.now(); const compra = { id: compraId, proveedor_id: datos.proveedor_id, fecha: datos.fecha, total: parseFloat(datos.total), monto_pagado: parseFloat(datos.total), estado: "pagado", fecha_creacion: new Date().toISOString() }; const promesasMateriales = []; for(let i=1; i<=5; i++) { const matId = datos[`mat_${i}`]; const cant = parseFloat(datos[`cant_${i}`] || 0); if(matId && cant > 0) { const material = App.state.inventario.find(m => m.id === matId); if(material) { const nuevoStock = parseFloat(material.stock_actual) + cant; promesasMateriales.push( App.api.fetch("actualizar_fila", { nombreHoja: "materiales", idFila: material.id, datosNuevos: { stock_actual: nuevoStock } }).then(()=> { material.stock_actual = nuevoStock; if (material.tipo === 'reventa') { const existeProd = App.state.productos.find(p => p.mat_1 === material.id); if(!existeProd) { const nuevoProd = { id: "PROD-" + Date.now() + i, nombre: material.nombre, categoria: "reventa", clasificacion: "Reventa", precio_venta: 0, mat_1: material.id, cant_1: 1, uso_1: "Completo", activo: "TRUE", fecha_creacion: new Date().toISOString() }; App.api.fetch("guardar_fila", { nombreHoja: "productos", datos: nuevoProd }).then(()=> App.state.productos.push(nuevoProd)); } } })); } } } const res = await App.api.fetch("guardar_fila", { nombreHoja: "compras", datos: compra }); if (res.status === "success") { await Promise.all(promesasMateriales); App.state.compras.push(compra); App.ui.hideLoader(); App.ui.toast("Compra registrada"); App.router.handleRoute(); } else { App.ui.hideLoader(); App.ui.toast("Error"); } },
        async guardarNuevaReparacion(datos) { App.ui.showLoader("Registrando..."); datos.id = "REP-" + Date.now(); datos.estado = "recibida"; datos.fecha_creacion = new Date().toISOString(); const res = await App.api.fetch("guardar_fila", { nombreHoja: "reparaciones", datos: datos }); App.ui.hideLoader(); if (res.status === "success") { App.state.reparaciones.push(datos); App.ui.toast("Reparación guardada"); App.router.handleRoute(); } else { App.ui.toast("Error Sheets."); } },
        async actualizarReparacion(repId, estado) { App.ui.showLoader("Actualizando..."); await App.api.fetch("actualizar_fila", { nombreHoja: "reparaciones", idFila: repId, datosNuevos: { estado: estado } }); const rep = App.state.reparaciones.find(r => r.id === repId); if(rep) rep.estado = estado; App.ui.hideLoader(); App.ui.toast("Actualizado"); App.router.handleRoute(); },
        async guardarAbono(datos) { App.ui.showLoader("Registrando..."); const nuevoAbono = { id: "ABO-" + Date.now(), pedido_id: datos.pedido_id, cliente_id: datos.cliente_id, monto: parseFloat(datos.monto) || 0, nota: datos.nota, fecha: new Date().toISOString() }; const res = await App.api.fetch("guardar_fila", { nombreHoja: "abonos_clientes", datos: nuevoAbono }); App.ui.hideLoader(); if (res.status === "success") { App.state.abonos.push(nuevoAbono); App.ui.toast("Pago registrado"); App.router.handleRoute(); } else App.ui.toast("Error"); },

        // --- GRAFICAS FINANZAS (FILTRO MES) ---
        renderGraficasFinanzas(filtro) {
            const cont = document.getElementById('finanzas-contenedor');
            if(!cont) return;
            const hoy = new Date(); let mesActual = hoy.getMonth(); let anioActual = hoy.getFullYear(); let mesPasado = mesActual - 1; let anioPasado = anioActual; if(mesPasado < 0) { mesPasado = 11; anioPasado--; }
            const filtrarFecha = (str) => { if(filtro === 'todo') return true; if(!str) return false; const f = new Date(str); if(filtro === 'mes_actual') return f.getMonth() === mesActual && f.getFullYear() === anioActual; if(filtro === 'mes_pasado') return f.getMonth() === mesPasado && f.getFullYear() === anioPasado; return true; };
            
            const pedFiltrados = App.state.pedidos.filter(p => filtrarFecha(p.fecha_creacion));
            const aboFiltrados = App.state.abonos.filter(a => filtrarFecha(a.fecha));
            const gasFiltrados = App.state.gastos.filter(g => filtrarFecha(g.fecha));

            const tVentas = pedFiltrados.reduce((acc, p) => acc + (parseFloat(p.total) || 0), 0); 
            const tAnticipos = pedFiltrados.reduce((acc, p) => acc + (parseFloat(p.anticipo) || 0), 0); 
            const tAbonos = aboFiltrados.reduce((acc, a) => acc + (parseFloat(a.monto) || 0), 0); 
            const ingReales = tAnticipos + tAbonos; 
            const xCobrar = tVentas - ingReales; 
            const tGastos = gasFiltrados.reduce((acc, g) => acc + (parseFloat(g.monto) || 0), 0); 
            const fNeto = ingReales - tGastos; 
            
            let html = `<div class="grid-2"><div class="card stat-card" style="background:#EBF8FF;"><div class="label">Ventas Totales</div><div class="value" style="color:#3182CE; font-size:1.2rem;">$${tVentas}</div></div><div class="card stat-card" style="background:#C6F6D5;"><div class="label">Ingresos Reales</div><div class="value" style="color:#38A169; font-size:1.2rem;">$${ingReales}</div></div><div class="card stat-card" style="background:#FEFCBF;"><div class="label">Por Cobrar</div><div class="value" style="color:#D69E2E; font-size:1.2rem;">$${xCobrar}</div></div><div class="card stat-card" style="background:#FED7D7;"><div class="label">Gastos</div><div class="value" style="color:#E53E3E; font-size:1.2rem;">$${tGastos}</div></div></div><div class="card stat-card" style="margin-top:10px; border:2px solid ${fNeto >= 0 ? 'var(--success)' : 'var(--danger)'};"><div class="label">Flujo Neto Efectivo</div><div class="value" style="color:${fNeto >= 0 ? 'var(--success)' : 'var(--danger)'};">$${fNeto}</div></div>`;
            html += `<h3 class="card-title" style="margin-top:20px;">Últimos Gastos Registrados</h3>`;
            if (gasFiltrados.length === 0) { html += `<p style="color:var(--text-muted); font-size:0.9rem;">No hay gastos en este periodo.</p>`; } 
            else { [...gasFiltrados].reverse().slice(0, 20).forEach(g => { html += `<div style="display: flex; justify-content: space-between; border-bottom: 1px solid var(--border); padding: 10px 0;"><div><strong style="font-size: 0.95rem;">${g.descripcion}</strong><span style="cursor:pointer; margin-left: 5px; font-size: 0.8rem;" onclick="App.views.formGasto('${g.id}')">✏️</span><span style="cursor:pointer; margin-left: 5px; font-size: 0.8rem; color:red;" onclick="App.logic.eliminarRegistroGenerico('gastos', '${g.id}', 'gastos')">🗑️</span><br><small style="color: var(--text-muted);">${g.categoria} | ${g.fecha}</small></div><strong style="color: var(--danger); font-size: 1rem;">-$${g.monto}</strong></div>`; }); }
            cont.innerHTML = html;
        },

        // --- PDFS ---
        imprimirNota(pedidoId) { 
            const p = App.state.pedidos.find(x => x.id === pedidoId); const c = App.state.clientes.find(cli => cli.id === p.cliente_id); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === p.id); const producto = detalle ? App.state.productos.find(prod => prod.id === detalle.producto_id) : null; const abonosDelPedido = App.state.abonos.filter(a => a.pedido_id === p.id); const totalAbonado = abonosDelPedido.reduce((s, a) => s + parseFloat(a.monto||0), 0); const saldoReal = parseFloat(p.total) - parseFloat(p.anticipo) - totalAbonado; 
            const ventana = window.open('', '_blank'); 
            let htmlNota = `<html><head><title>Nota</title><style>body{font-family:sans-serif;background:#f9f9f9;padding:20px;color:#333;}.ticket{background:white;max-width:380px;margin:0 auto;padding:25px;border-radius:8px;box-shadow:0 4px 15px rgba(0,0,0,0.05);}.header{text-align:center;border-bottom:2px dashed #cbd5e0;padding-bottom:15px;margin-bottom:15px;}.header img{max-height:80px;margin-bottom:10px;}.header h1{margin:0;color:#2d3748;font-size:24px;text-transform:uppercase;}.info-p{margin:4px 0;font-size:14px;}table{width:100%;border-collapse:collapse;margin-top:15px;margin-bottom:15px;}th{border-bottom:1px solid #e2e8f0;padding:8px 0;text-align:left;font-size:13px;color:#718096;}td{padding:8px 0;font-size:14px;color:#2d3748;}.totales{border-top:2px solid #cbd5e0;padding-top:15px;}.totales-row{display:flex;justify-content:space-between;margin-bottom:5px;font-size:14px;}.saldo-final{display:flex;justify-content:space-between;margin-top:10px;font-size:18px;font-weight:bold;color:#2d3748;background:${saldoReal<=0?'#f0fff4':'#fff5f5'};padding:10px;border-radius:6px;}.footer{text-align:center;margin-top:30px;font-size:12px;color:#a0aec0;}.social{margin-top:10px;font-weight:bold;color:#4A5568;font-size:13px;}@media print{body{background:white;padding:0;}.ticket{box-shadow:none;border:none;max-width:100%;}}</style></head><body><div class="ticket"><div class="header"><img src="${App.state.config.logoUrl}" onerror="this.style.display='none'"><h1>${App.state.config.empresa}</h1><p>Artesanía Yucateca</p></div><div style="margin-bottom:20px;"><p class="info-p"><strong>Folio:</strong> ${p.id.replace('PED-','')}</p><p class="info-p"><strong>Fecha:</strong> ${new Date(p.fecha_creacion).toLocaleDateString()}</p><p class="info-p"><strong>Cliente:</strong> ${c?c.nombre:'Mostrador'}</p></div><table><tr><th>Cant</th><th>Desc</th><th style="text-align:right;">Importe</th></tr><tr><td>${detalle?detalle.cantidad:1}</td><td>${producto?producto.nombre:'Artículo'}<br><small style="color:#718096;font-size:11px;">${p.notas||''}</small></td><td style="text-align:right;">$${p.total}</td></tr></table><div class="totales"><div class="totales-row"><span>Subtotal:</span> <span>$${p.total}</span></div><div class="totales-row"><span>Anticipo:</span> <span style="color:#e53e3e;">-$${p.anticipo}</span></div>${totalAbonado>0?`<div class="totales-row"><span>Abonos:</span> <span style="color:#e53e3e;">-$${totalAbonado}</span></div>`:''}<div class="saldo-final"><span>SALDO:</span><span>$${saldoReal>0?saldoReal:0}</span></div></div><div class="footer"><p style="font-size:14px;font-weight:bold;color:#E53E3E;">¡gracias por comprar lo hecho con amor! ❤️</p><p class="social">👉 siguenos en nuestras redes sociales:<br>${App.state.config.redesSociales}</p><p style="margin-top:15px;">conserva tu recibo para aclaraciones</p></div></div><script>setTimeout(()=>{window.print();},500);<\/script></body></html>`; 
            ventana.document.write(htmlNota); ventana.document.close(); 
        },
        imprimirCotizacion(cotId) { 
            const c = App.state.cotizaciones.find(x => x.id === cotId); const ventana = window.open('', '_blank'); 
            let htmlNota = `<html><head><title>Cotización</title><style>body{font-family:sans-serif;background:#f9f9f9;padding:20px;color:#333;}.ticket{background:white;max-width:380px;margin:0 auto;padding:25px;border-radius:8px;box-shadow:0 4px 15px rgba(0,0,0,0.05);}.header{text-align:center;border-bottom:2px dashed #cbd5e0;padding-bottom:15px;margin-bottom:15px;}.header img{max-height:80px;margin-bottom:10px;}.header h1{margin:0;color:#2d3748;font-size:24px;text-transform:uppercase;}.info-p{margin:4px 0;font-size:14px;}table{width:100%;border-collapse:collapse;margin-top:15px;margin-bottom:15px;}th{border-bottom:1px solid #e2e8f0;padding:8px 0;text-align:left;font-size:13px;color:#718096;}td{padding:8px 0;font-size:14px;color:#2d3748;}.totales{border-top:2px solid #cbd5e0;padding-top:15px;}.saldo-final{display:flex;justify-content:space-between;margin-top:10px;font-size:18px;font-weight:bold;color:#B7791F;background:#FFFBEB;padding:10px;border-radius:6px;}.footer{text-align:center;margin-top:30px;font-size:12px;color:#a0aec0;}.social{margin-top:10px;font-weight:bold;color:#4A5568;font-size:13px;}@media print{body{background:white;padding:0;}.ticket{box-shadow:none;border:none;max-width:100%;}}</style></head><body><div class="ticket"><div class="header"><img src="${App.state.config.logoUrl}" onerror="this.style.display='none'"><h1>${App.state.config.empresa}</h1><h2 style="color:#D69E2E;font-size:18px;">COTIZACIÓN</h2></div><div style="margin-bottom:20px;"><p class="info-p"><strong>Folio:</strong> ${c.id.replace('COT-','')}</p><p class="info-p"><strong>Fecha:</strong> ${new Date(c.fecha_creacion).toLocaleDateString()}</p><p class="info-p"><strong>Cliente:</strong> ${c.cliente_nombre}</p></div><table><tr><th>Cant</th><th>Desc</th><th style="text-align:right;">Importe</th></tr><tr><td>1</td><td>${c.descripcion}</td><td style="text-align:right;">$${c.total}</td></tr></table><div class="totales"><div class="saldo-final"><span>TOTAL COTIZADO:</span><span>$${c.total}</span></div></div><div class="footer"><p>Vigencia de cotización: 15 días.</p><p class="social">👉 siguenos en nuestras redes sociales:<br>${App.state.config.redesSociales}</p></div></div><script>setTimeout(()=>{window.print();},500);<\/script></body></html>`; 
            ventana.document.write(htmlNota); ventana.document.close(); 
        },
        enviarWhatsApp(pedidoId) { const p = App.state.pedidos.find(x => x.id === pedidoId); const c = App.state.clientes.find(cli => cli.id === p.cliente_id); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === p.id); const producto = detalle ? App.state.productos.find(prod => prod.id === detalle.producto_id) : null; const abonos = App.state.abonos.filter(a => a.pedido_id === p.id).reduce((s, a) => s + parseFloat(a.monto||0), 0); const saldoReal = parseFloat(p.total) - parseFloat(p.anticipo) - abonos; if(!c || !c.telefono) return; let texto = `Hola *${c.nombre}* 👋,\nSomos de *${App.state.config.empresa}*.\n\nDetalle de tu pedido:\n📦 *Producto:* ${producto ? producto.nombre : 'Hamaca'}\n💰 *Total:* $${p.total}\n✅ *Abonado:* $${parseFloat(p.anticipo) + abonos}\n⚠️ *Saldo Pendiente:* $${saldoReal > 0 ? saldoReal : 0}\n\n👉 Síguenos: ${App.state.config.redesSociales}\n¡Gracias!`; let tel = String(c.telefono).replace(/\D/g,''); if(tel.length === 10) tel = '52' + tel; window.location.href = `https://wa.me/${tel}?text=${encodeURIComponent(texto)}`; }
    },

    views: {
        login() { return `<div style="display:flex; flex-direction:column; align-items:center; justify-content:center; height:80vh; text-align:center;"><div style="background:var(--primary); color:white; width:80px; height:80px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-size:2rem; margin-bottom:20px;">🔒</div><h2 style="margin-bottom:10px; color:var(--primary-dark);">Descanso Maya</h2><p style="color:var(--text-muted); margin-bottom:30px;">Ingresa el PIN</p><div class="card" style="width:100%; max-width:320px;"><input type="password" id="pin-input" placeholder="PIN" style="width:100%; padding:12px; font-size:1.2rem; text-align:center; border:1px solid var(--border); border-radius:8px; margin-bottom:15px;"><button class="btn btn-primary" style="width:100%; padding:12px; font-size:1rem;" onclick="App.logic.verificarPIN(document.getElementById('pin-input').value)">Entrar</button></div></div>`; },
        
        inicio() { 
            document.getElementById('bottom-nav').style.display = 'flex'; 
            const pedidosCount = App.state.pedidos ? App.state.pedidos.length : 0; 
            const prodCount = App.state.ordenes_produccion ? App.state.ordenes_produccion.filter(o => o.estado !== 'listo').length : 0; 
            
            // Alertas de Stock Bajo
            let alertasHTML = '';
            const stockBajo = App.state.inventario.filter(i => parseFloat(i.stock_minimo) > 0 && parseFloat(i.stock_actual) <= parseFloat(i.stock_minimo));
            if(stockBajo.length > 0) {
                alertasHTML = `<div style="background:#FED7D7; border-left:4px solid #E53E3E; padding:10px; margin-bottom:15px; border-radius:4px;"><strong style="color:#E53E3E;">⚠️ Alerta de Stock Bajo:</strong><ul style="margin:5px 0 0 20px; color:#C53030; font-size:0.85rem;">`;
                stockBajo.forEach(i => { alertasHTML += `<li>${i.nombre}: Quedan ${i.stock_actual} ${i.unidad} (Mínimo: ${i.stock_minimo})</li>`; });
                alertasHTML += `</ul></div>`;
            }

            return `${alertasHTML}<div class="grid-2"><div class="card stat-card"><div class="label">Pedidos</div><div class="value">${pedidosCount}</div></div><div class="card stat-card"><div class="label">Producción</div><div class="value">${prodCount}</div></div><div class="card stat-card" onclick="App.router.navigate('nomina')" style="cursor:pointer; background:#FFF5F5;"><div class="label" style="color:#E53E3E;">Nómina</div><div class="value">🧑‍🎨</div></div><div class="card stat-card" onclick="App.router.navigate('finanzas')" style="cursor:pointer; background:#EBF8FF;"><div class="label" style="color:#3182CE;">Ver Finanzas</div><div class="value">📊</div></div></div>`; 
        },
        
        inventario() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Bodega / Inventario</h3><input type="text" id="bus-inv" onkeyup="window.filtrarLista('bus-inv', 'fila-inv')" placeholder="🔍 Buscar insumo..." style="width:100%; padding:8px; margin-bottom:15px; border-radius:6px; border:1px solid var(--border);">`; if (!App.state.inventario || App.state.inventario.length === 0) { html += `<p>No hay insumos.</p>`; } else { html += `<table style="width:100%; border-collapse: collapse; font-size: 0.9rem;"><tr style="border-bottom: 2px solid var(--border);"><th style="text-align:left; padding:8px;">Artículo / Tipo</th><th style="padding:8px;">Stock</th><th></th></tr>`; App.state.inventario.forEach(i => { html += `<tr class="fila-inv" style="border-bottom: 1px solid var(--border);"><td style="padding: 10px 5px;"><strong>${i.nombre}</strong><br><small style="color:var(--primary); text-transform:uppercase; font-size:0.75rem;">[${i.tipo || 'OTRO'}]</small> <small style="color:var(--text-muted)">${i.unidad}</small></td><td style="padding: 10px 5px; text-align:center; font-weight:bold; font-size:1.1rem; color:${parseFloat(i.stock_minimo)>0 && parseFloat(i.stock_actual)<=parseFloat(i.stock_minimo) ? 'var(--danger)' : 'var(--text-main)'}">${i.stock_actual}</td><td style="text-align:right;"><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formMaterial('${i.id}')">✏️</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarRegistroGenerico('materiales', '${i.id}', 'inventario')">🗑️</button></td></tr>`; }); html += `</table>`; } html += `</div><button class="fab" onclick="App.views.formMaterial()">+</button>`; return html; },
        productos() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Catálogo de Productos</h3><input type="text" id="bus-prod" onkeyup="window.filtrarLista('bus-prod', 'tarj-prod')" placeholder="🔍 Buscar producto..." style="width:100%; padding:8px; margin-bottom:15px; border-radius:6px; border:1px solid var(--border);">`; if (!App.state.productos || App.state.productos.length === 0) { html += `<p style="color: var(--text-muted);">No hay productos registrados.</p>`; } else { App.state.productos.forEach(p => { html += `<div class="card tarj-prod" style="border: 1px solid var(--border); box-shadow: none; padding: 12px; display:flex; justify-content:space-between; align-items:center;"><div style="flex:1;"><strong>${p.nombre}</strong><br><small style="color: var(--text-muted);">Cat: ${p.categoria || 'General'} | Clasif: ${p.clasificacion || 'Normal'}</small></div><div style="text-align:right;"><span style="color: var(--primary); font-weight: bold; display:block; margin-bottom:5px;">$${p.precio_venta} <br><small style="color:#D69E2E;">Mayoreo: $${p.precio_mayoreo||'N/A'}</small></span><div><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formProducto('${p.id}')">✏️</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarRegistroGenerico('productos', '${p.id}', 'productos')">🗑️</button></div></div></div>`; }); } html += `</div><button class="fab" onclick="App.views.formProducto()">+</button>`; return html; },
        pedidos() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Historial de Pedidos</h3><input type="text" id="bus-ped" onkeyup="window.filtrarLista('bus-ped', 'tarj-ped')" placeholder="🔍 Buscar por nombre o folio..." style="width:100%; padding:8px; margin-bottom:15px; border-radius:6px; border:1px solid var(--border);">`; [...(App.state.pedidos||[])].reverse().slice(0, 80).forEach(p => { const c = App.state.clientes.find(cli => cli.id === p.cliente_id); const nombreC = p.cliente_id === "STOCK_INTERNO" ? "📦 BODEGA (Stock)" : (c ? c.nombre : 'Mostrador'); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === p.id); const producto = detalle ? App.state.productos.find(prod => prod.id === detalle.producto_id) : null; const tieneOrden = detalle ? App.state.ordenes_produccion.some(o => o.pedido_detalle_id === detalle.id) : false; const abonos = App.state.abonos.filter(a => a.pedido_id === p.id).reduce((sum, a) => sum + parseFloat(a.monto || 0), 0); const saldoReal = parseFloat(p.total || 0) - parseFloat(p.anticipo || 0) - abonos; const estaPagado = saldoReal <= 0; html += `<div class="card tarj-ped" style="border: 1px solid var(--border); box-shadow: none;"><div style="display: flex; justify-content: space-between; margin-bottom: 8px;"><strong>${p.id} - ${nombreC}</strong>${estaPagado ? `<span class="badge" style="background: var(--success); color: white;">PAGADO</span>` : `<span class="badge ${p.estado || 'nuevo'}">${(p.estado || 'Nuevo').toUpperCase()}</span>`}</div><p style="color: var(--primary); font-size: 0.9rem; font-weight: bold; margin-bottom: 5px;">${producto ? producto.nombre : 'Desconocido'}</p><div style="display: flex; justify-content: space-between; align-items: center; margin-top: 10px; padding-top: 10px; border-top: 1px dashed var(--border);"><div><strong style="color: ${estaPagado ? 'var(--success)' : 'var(--danger)'};">Saldo: $${saldoReal > 0 ? saldoReal : 0}</strong></div><div style="display: flex; gap: 8px;">${!tieneOrden && detalle && (!producto || producto.categoria !== 'reventa') ? `<button class="btn btn-secondary" style="padding: 6px 10px;" onclick="App.logic.mandarAProduccion('${p.id}')">🔨</button>` : ''}${!estaPagado && p.cliente_id !== "STOCK_INTERNO" ? `<button class="btn btn-primary" style="padding: 6px 10px; background: var(--success); border-color: var(--success);" onclick="App.views.formCobrar('${p.id}', '${c ? c.id : ''}', ${saldoReal})">💰</button>` : ''}</div></div><div style="display: flex; gap: 10px; margin-top: 15px;">${p.cliente_id !== "STOCK_INTERNO" ? `<button class="btn" style="flex: 1; font-size: 0.8rem; border-color: #38A169; color: #38A169;" onclick="App.logic.enviarWhatsApp('${p.id}')">💬 WApp</button><button class="btn" style="flex: 1; font-size: 0.8rem; border-color: var(--primary); color: var(--primary);" onclick="App.logic.imprimirNota('${p.id}')">🖨️ PDF</button>` : ''}<button class="btn btn-secondary" style="font-size: 0.8rem; padding: 6px 10px;" onclick="App.views.formEditarPedido('${p.id}')">✏️</button><button class="btn btn-secondary" style="font-size: 0.8rem; padding: 6px 10px; color: red; border-color: red;" onclick="App.logic.eliminarPedido('${p.id}')">🗑️</button></div></div>`; }); return html += `</div><button class="fab" onclick="App.views.formNuevoPedido()">+</button>`; },
        
        produccion() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Tablero Kanban</h3><button class="btn btn-primary" style="width:100%; margin-top:10px; background:#D69E2E; border-color:#D69E2E;" onclick="App.views.formOrdenStock()">+ 🔨 Fabricar para Bodega</button></div>`; const pendientes = (App.state.ordenes_produccion||[]).filter(o => o.estado === 'pendiente' || !o.estado); const enProceso = (App.state.ordenes_produccion||[]).filter(o => o.estado === 'en_proceso'); const listas = (App.state.ordenes_produccion||[]).filter(o => o.estado === 'listo'); const dibujarTarjeta = (orden) => { const detalle = App.state.pedido_detalle.find(d => d.id === orden.pedido_detalle_id); const producto = detalle ? App.state.productos.find(p => p.id === detalle.producto_id) : null; const pedido = detalle ? App.state.pedidos.find(p => p.id === detalle.pedido_id) : null; let nombreCliente = 'Sin Cliente'; if(pedido && pedido.cliente_id === "STOCK_INTERNO") { nombreCliente = "📦 BODEGA"; } else if (pedido) { const clienteObj = App.state.clientes.find(c => c.id === pedido.cliente_id); if(clienteObj) nombreCliente = clienteObj.nombre; } const pagos = App.state.pago_artesanos.filter(p => p.orden_id === orden.id); let infoArtesanos = pagos.length > 0 ? `🛠️ ${pagos.length} Trabajos` : '👤 Sin asignar'; let semaforo = ''; if(pedido && pedido.fecha_entrega && orden.estado !== 'listo') { const fEnt = new Date(pedido.fecha_entrega+'T00:00:00'); const hoy = new Date(); hoy.setHours(0,0,0,0); const d = Math.ceil((fEnt - hoy)/(1000*60*60*24)); if(d < 0) semaforo = '<span style="background:red; color:white; padding:2px 6px; border-radius:4px; font-size:0.7rem; margin-left:5px;">🔴 Atrasado</span>'; else if(d <= 1) semaforo = '<span style="background:orange; color:white; padding:2px 6px; border-radius:4px; font-size:0.7rem; margin-left:5px;">🟡 Urgente</span>'; else semaforo = '<span style="background:green; color:white; padding:2px 6px; border-radius:4px; font-size:0.7rem; margin-left:5px;">🟢 A tiempo</span>'; } return `<div class="kanban-card" onclick="App.views.modalEditarOrden('${orden.id}')"><div style="display:flex; justify-content:space-between; margin-bottom:5px;"><strong>${orden.id}</strong><div><span style="font-size:0.75rem; color:#718096; font-weight:bold;">${pedido ? pedido.id.replace('PED-','') : ''}</span>${semaforo}</div></div><small style="color: var(--primary); font-weight: 600; display:block;">${producto ? producto.nombre : 'Producto interno'}</small><small style="color: #4A5568; display:block; margin-bottom:5px; font-weight:bold;">👤 ${nombreCliente}</small><div style="margin-top: 8px; font-size: 0.8rem; color: var(--text-muted); display:flex; justify-content: space-between; border-top: 1px dashed #E2E8F0; padding-top: 5px;"><span>${infoArtesanos}</span></div></div>`; }; html += `<div class="kanban-board"><div class="kanban-column"><div class="kanban-header">Pendientes <span>${pendientes.length}</span></div>${pendientes.map(dibujarTarjeta).join('')}</div><div class="kanban-column"><div class="kanban-header">En Proceso <span>${enProceso.length}</span></div>${enProceso.map(dibujarTarjeta).join('')}</div><div class="kanban-column"><div class="kanban-header">Listas <span>${listas.length}</span></div>${listas.map(dibujarTarjeta).join('')}</div></div>`; return html; },
        nomina() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Nómina de Artesanos</h3>`; const pagosPorArtesano = {}; App.state.artesanos.forEach(a => { pagosPorArtesano[a.id] = { nombre: a.nombre, totalPendiente: 0, trabajos: [] }; }); App.state.pago_artesanos.forEach(p => { if (p.estado === 'pendiente' && pagosPorArtesano[p.artesano_id]) { pagosPorArtesano[p.artesano_id].totalPendiente += parseFloat(p.total); pagosPorArtesano[p.artesano_id].trabajos.push(p); } }); let hayPendientes = false; for (const [id, data] of Object.entries(pagosPorArtesano)) { if (data.totalPendiente > 0) { hayPendientes = true; html += `<div class="card" style="border: 1px solid var(--border); box-shadow: none;"><div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px;"><strong>🧑‍🎨 ${data.nombre}</strong><strong style="color: var(--danger); font-size: 1.1rem;">$${data.totalPendiente}</strong></div><button class="btn btn-primary" style="width: 100%; background: var(--success); border-color: var(--success);" onclick="if(confirm('¿Liquidar $${data.totalPendiente} a ${data.nombre}?')) App.logic.liquidarNomina('${id}')">💵 Registrar Pago / Liquidar</button></div>`; } } if (!hayPendientes) html += `<p style="color: var(--text-muted);">No hay pagos pendientes. Todo está al día. ✨</p>`; html += `</div>`; return html; },
        
        finanzas() { 
            document.getElementById('bottom-nav').style.display = 'flex'; 
            setTimeout(() => App.logic.renderGraficasFinanzas('todo'), 50); 
            return `<div class="card"><div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:15px;"><h3 class="card-title" style="margin:0;">Finanzas</h3><select id="filtro-finanzas" onchange="App.logic.renderGraficasFinanzas(this.value)" style="padding:5px; border-radius:5px;"><option value="todo">Todo el historial</option><option value="mes_actual">Este Mes</option><option value="mes_pasado">Mes Pasado</option></select></div><div id="finanzas-contenedor">Cargando datos...</div></div><button class="fab" style="background: var(--danger);" onclick="App.views.formGasto()">+</button>`; 
        },
        
        reparaciones() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Reparaciones</h3>`; [...App.state.reparaciones].reverse().forEach(r => { const c = App.state.clientes.find(cli => cli.id === r.cliente_id); const pagos = App.state.pago_artesanos.filter(p => p.orden_id === r.id); let infoArtesanos = pagos.length > 0 ? `🛠️ ${pagos.length} Trabajos asignados` : '👤 Sin asignar'; const estaLista = r.estado === 'entregada'; html += `<div class="card" style="border: 1px solid var(--border); box-shadow: none;"><div style="display: flex; justify-content: space-between; margin-bottom: 8px;"><strong>${r.id} - ${c ? c.nombre : 'Cliente'}</strong><span class="badge ${estaLista ? 'listo' : 'pendiente'}">${r.estado.toUpperCase()}</span></div><p style="color: var(--primary); font-size: 0.9rem; font-weight: bold; margin-bottom: 5px;">${r.descripcion}</p><div style="font-size:0.8rem; margin-bottom:10px; color:#4A5568;">${infoArtesanos}</div><div style="display:flex; justify-content:space-between; gap:5px;">`; if(!estaLista) { html += `<button class="btn btn-secondary" style="font-size:0.8rem; padding:6px; flex:1;" onclick="App.views.formAgregarTrabajo('${r.id}', true)">+ Asignar Tarea</button>`; html += `<button class="btn btn-primary" style="font-size:0.8rem; padding:6px; background:var(--success); border-color:var(--success); flex:1;" onclick="App.logic.actualizarReparacion('${r.id}', 'entregada')">✔ Marcar Lista</button>`; } html += `<button class="btn btn-secondary" style="padding:6px; font-size:0.8rem; color:red;" onclick="App.logic.eliminarRegistroGenerico('reparaciones', '${r.id}', 'reparaciones')">🗑️</button></div></div>`; }); return html += `</div><button class="fab" onclick="App.views.formNuevaReparacion()">+</button>`; },
        cotizaciones() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Cotizaciones Rápidas</h3><p style="color:var(--text-muted); font-size:0.85rem; margin-bottom:15px;">Solo informativas. No afectan inventario ni finanzas.</p>`; if (!App.state.cotizaciones || App.state.cotizaciones.length === 0) { html += `<p>No hay cotizaciones recientes.</p>`; } else { [...App.state.cotizaciones].reverse().forEach(c => { html += `<div class="card" style="border: 1px solid var(--border); box-shadow: none;"><div style="display: flex; justify-content: space-between; margin-bottom: 5px;"><strong>${c.id} - ${c.cliente_nombre}</strong><span style="color: var(--primary); font-weight: bold;">$${c.total}</span></div><p style="color: var(--text-muted); font-size: 0.85rem; margin-bottom: 5px;">${c.descripcion}</p><div style="display:flex; gap:5px; justify-content:flex-end;"><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.logic.imprimirCotizacion('${c.id}')">🖨️ Imprimir PDF</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarCotizacion('${c.id}')">🗑️</button></div></div>`; }); } html += `</div><button class="fab" onclick="App.views.formNuevaCotizacion()">+</button>`; return html; },
        clientes() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Directorio de Clientes</h3><input type="text" id="bus-cli" onkeyup="window.filtrarLista('bus-cli', 'tarj-cli')" placeholder="🔍 Buscar cliente..." style="width:100%; padding:8px; margin-bottom:15px; border-radius:6px; border:1px solid var(--border);">`; App.state.clientes.forEach(c => { html += `<div class="card tarj-cli" style="border: 1px solid var(--border); padding: 10px; margin-bottom: 8px; display:flex; justify-content:space-between; align-items:center;"><div><strong>${c.nombre}</strong><br><small style="color: var(--text-muted);">📞 ${c.telefono || 'N/A'}</small></div><div><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formCliente('${c.id}')">✏️</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarRegistroGenerico('clientes', '${c.id}', 'clientes')">🗑️</button></div></div>`; }); html += `</div><button class="fab" onclick="App.views.formCliente()">+</button>`; return html; },
        proveedores() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Proveedores</h3>`; App.state.proveedores.forEach(p => { html += `<div class="card" style="border: 1px solid var(--border); padding: 10px; margin-bottom: 8px; display:flex; justify-content:space-between; align-items:center;"><div><strong>${p.nombre}</strong><br><small style="color: var(--text-muted);">📞 ${p.telefono || 'N/A'}</small></div><div><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formProveedor('${p.id}')">✏️</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarRegistroGenerico('proveedores', '${p.id}', 'proveedores')">🗑️</button></div></div>`; }); html += `</div><button class="fab" onclick="App.views.formProveedor()">+</button>`; return html; },
        artesanos() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Artesanos y Tareas</h3>`; App.state.artesanos.forEach(a => { html += `<div class="card" style="border: 1px solid var(--border); padding: 10px; margin-bottom: 8px; display:flex; justify-content:space-between; align-items:center;"><div><strong>${a.nombre}</strong><br><small style="color: var(--text-muted);">📞 ${a.telefono || 'N/A'}</small></div><div><button class="btn btn-primary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px; background:#D69E2E; border-color:#D69E2E;" onclick="App.views.verTarifasArtesano('${a.id}')">Tarifas 💲</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formArtesano('${a.id}')">✏️</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarRegistroGenerico('artesanos', '${a.id}', 'artesanos')">🗑️</button></div></div>`; }); html += `</div><button class="fab" onclick="App.views.formArtesano()">+</button>`; return html; },
        verTarifasArtesano(artesanoId) { const artesano = App.state.artesanos.find(a => a.id === artesanoId); const tarifas = App.state.tarifas_artesano.filter(t => t.artesano_id === artesanoId); let html = `<div style="margin-bottom: 15px;">Gestiona cuánto cobra <strong>${artesano.nombre}</strong> por cada tipo de tarea.</div><table style="width:100%; border-collapse: collapse; font-size: 0.9rem; margin-bottom:15px;"><tr style="border-bottom: 2px solid var(--border);"><th style="text-align:left; padding:8px;">Tipo de Trabajo</th><th style="padding:8px;">Monto</th><th></th></tr>`; if(tarifas.length === 0) { html += `<tr><td colspan="3" style="padding:10px; color:#aaa; text-align:center;">Sin tareas configuradas</td></tr>`; } else { tarifas.forEach(t => { html += `<tr style="border-bottom: 1px solid var(--border);"><td style="padding: 10px 5px;">${t.clasificacion}</td><td style="padding: 10px 5px; text-align:center; font-weight:bold; color:var(--success)">$${t.monto}</td><td style="text-align:right;"><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border:color:red;" onclick="App.logic.eliminarRegistroGenerico('tarifas_artesano', '${t.id}', 'tarifas_artesano'); App.ui.closeSheet();">🗑️</button></td></tr>`; }); } html += `</table><button class="btn btn-primary" style="width:100%;" onclick="App.ui.closeSheet(); setTimeout(()=>App.views.formTarifa('${artesanoId}'), 400);">+ Agregar Tarea</button>`; App.ui.openSheet(`Tarifas de ${artesano.nombre}`, html); },
        compras() { document.getElementById('bottom-nav').style.display = 'flex'; let html = `<div class="card"><h3 class="card-title">Historial de Compras</h3>`; [...(App.state.compras||[])].reverse().forEach(c => { const p = App.state.proveedores.find(prv => prv.id === c.proveedor_id); html += `<div class="card" style="border: 1px solid var(--border); box-shadow: none;"><div style="display: flex; justify-content: space-between; margin-bottom: 5px;"><strong>${c.id}</strong><span style="color: var(--danger); font-weight: bold;">$${c.total}</span></div><p style="color: var(--text-muted); font-size: 0.85rem; margin-bottom: 5px;">Proveedor: ${p ? p.nombre : 'General'} | Fecha: ${c.fecha}<br><em>(Varios Insumos)</em></p><div style="text-align: right;"><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; margin-right:4px;" onclick="App.views.formEditarCompra('${c.id}')">✏️ Editar</button><button class="btn btn-secondary" style="padding:4px 8px; font-size:0.8rem; color:red; border-color:red;" onclick="App.logic.eliminarCompra('${c.id}')">🗑️ Eliminar</button></div></div>`; }); return html += `</div><button class="fab" onclick="App.views.formCompra()">+</button>`; },
        configuracion() { document.getElementById('bottom-nav').style.display = 'flex'; return `<div class="card"><h3 class="card-title">Configuración</h3><button class="btn btn-primary" style="width: 100%; margin-bottom: 15px;" onclick="App.logic.descargarRespaldo()">💾 Descargar Respaldo</button><button class="btn btn-secondary" style="width: 100%; background: #FED7D7; color: var(--danger); border: none;" onclick="localStorage.removeItem('erp_pin'); location.reload();">🔒 Bloquear Sistema</button></div>`; },
        mas() { return `<div class="grid-2"><div class="card stat-card" style="cursor:pointer;" onclick="App.router.navigate('clientes')"><div class="label" style="margin-top:0;">👥 Clientes</div></div><div class="card stat-card" style="cursor:pointer;" onclick="App.router.navigate('proveedores')"><div class="label" style="margin-top:0;">🚚 Proveedores</div></div><div class="card stat-card" style="cursor:pointer;" onclick="App.router.navigate('artesanos')"><div class="label" style="margin-top:0;">🧑‍🎨 Artesanos / Tareas</div></div><div class="card stat-card" style="cursor:pointer; background: #FFFBEB;" onclick="App.router.navigate('inventario')"><div class="label" style="margin-top:0; color:#D69E2E;">📦 Bodega / Insumos</div></div><div class="card stat-card" style="cursor:pointer;" onclick="App.router.navigate('productos')"><div class="label" style="margin-top:0;">🧶 Productos / Recetas</div></div><div class="card stat-card" style="cursor:pointer; background: #EBF8FF;" onclick="App.router.navigate('compras')"><div class="label" style="margin-top:0; color:#3182CE;">🛒 Ingresar Compra</div></div><div class="card stat-card" style="cursor:pointer; background: #FAF5FF;" onclick="App.router.navigate('reparaciones')"><div class="label" style="margin-top:0; color:#6B46C1;">🪡 Reparaciones</div></div><div class="card stat-card" style="cursor:pointer; background: #EEF2FF;" onclick="App.router.navigate('cotizaciones')"><div class="label" style="margin-top:0; color:#4C51BF;">📝 Cotizaciones</div></div><div class="card stat-card" style="cursor:pointer; background: #EDF2F7; grid-column: span 2;" onclick="App.router.navigate('configuracion')"><div class="label" style="margin-top:0; color: #4A5568;">⚙️ Configuración</div></div></div>`; },

        formNuevaCotizacion() { const formHTML = `<form id="dynamic-form"><div style="background: #FFFBEB; padding: 10px; border-radius: 8px; margin-bottom: 15px; font-size: 0.85rem; color:#D69E2E;">📝 Las cotizaciones no restan inventario ni se suman a las finanzas.</div><div class="form-group"><label>Nombre del Cliente</label><input type="text" name="cliente_nombre" required></div><div class="form-group"><label>Descripción (Ej. Hamaca Matrimonial)</label><input type="text" name="descripcion" required></div><div class="form-group"><label>Precio Cotizado ($)</label><input type="number" step="0.01" name="total" required></div><button type="submit" class="btn btn-primary" style="width: 100%;">Generar Cotización y PDF</button></form>`; App.ui.openSheet("Nueva Cotización", formHTML, (data) => App.logic.guardarCotizacion(data)); },
        formMaterial(id = null) { const obj = id ? App.state.inventario.find(m => m.id === id) : null; const formHTML = `<form id="dynamic-form"><div style="background: #EBF8FF; padding: 10px; border-radius: 8px; margin-bottom: 15px; font-size: 0.85rem;">Crea un insumo (Ej. Hilo Rojo, Argollas, o Hamaca para Reventa).</div><div class="form-group"><label>Nombre del Insumo / Artículo</label><input type="text" name="nombre" value="${obj ? obj.nombre : ''}" required placeholder="Ej. Hilo Nylon Rojo"></div><div class="form-group"><label>Tipo de Insumo</label><select name="tipo" required><option value="hilo" ${obj && obj.tipo === 'hilo' ? 'selected' : ''}>Hilo para Tejer</option><option value="accesorio" ${obj && obj.tipo === 'accesorio' ? 'selected' : ''}>Accesorios (Argollas, etc)</option><option value="reventa" ${obj && obj.tipo === 'reventa' ? 'selected' : ''}>Hamaca Terminada (Reventa)</option><option value="otro" ${obj && obj.tipo === 'otro' ? 'selected' : ''}>Otro</option></select></div><div class="form-group"><label>Unidad de Medida</label><select name="unidad"><option value="Tubos" ${obj && obj.unidad === 'Tubos' ? 'selected' : ''}>Tubos</option><option value="Kg" ${obj && obj.unidad === 'Kg' ? 'selected' : ''}>Kilogramos (Kg)</option><option value="Pzas" ${obj && obj.unidad === 'Pzas' ? 'selected' : ''}>Piezas (Pzas)</option></select></div><div class="grid-2"><div class="form-group"><label>Stock Actual</label><input type="number" step="0.1" name="stock_actual" value="${obj ? obj.stock_actual : '0'}" required></div><div class="form-group"><label>Stock Mínimo (Alerta)</label><input type="number" step="0.1" name="stock_minimo" value="${obj ? (obj.stock_minimo||'0') : '0'}" required></div></div><button type="submit" class="btn btn-primary" style="width: 100%;">${obj ? 'Guardar Cambios' : 'Crear Insumo'}</button></form>`; App.ui.openSheet(obj ? "Editar Insumo" : "Nuevo Insumo", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("materiales", id, data, "inventario"); else App.logic.guardarNuevoGenerico("materiales", data, "MAT", "inventario"); }); },
        formProducto(id = null) { const obj = id ? App.state.productos.find(p => p.id === id) : null; const generarSelect = (name, val) => `<select name="${name}"><option value="">-- Ninguno --</option>` + App.state.inventario.map(m => `<option value="${m.id}" ${val === m.id ? 'selected' : ''}>${m.nombre} (${m.unidad})</option>`).join('') + `</select>`; const generarUso = (name, val) => `<select name="${name}"><option value="">--</option><option value="Cuerpo" ${val==='Cuerpo'?'selected':''}>Cuerpo</option><option value="Brazos" ${val==='Brazos'?'selected':''}>Brazos</option><option value="Adicional" ${val==='Adicional'?'selected':''}>Otro</option></select>`; const clasif = obj ? obj.clasificacion : ''; let recetaHTML = ''; for(let i=1; i<=5; i++) { recetaHTML += `<div class="grid-3" style="margin-bottom: 5px;"><div class="form-group" style="margin:0;"><label>Insumo ${i}</label>${generarSelect(`mat_${i}`, obj ? obj[`mat_${i}`] : '')}</div><div class="form-group" style="margin:0;"><label>Cant.</label><input type="number" step="0.1" name="cant_${i}" value="${obj ? obj[`cant_${i}`] || '' : ''}"></div><div class="form-group" style="margin:0;"><label>Uso</label>${generarUso(`uso_${i}`, obj ? obj[`uso_${i}`] : '')}</div></div>`; } const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Nombre del Producto a Vender</label><input type="text" name="nombre" value="${obj ? obj.nombre : ''}" required></div><div class="grid-2"><div class="form-group"><label>Categoría</label><select name="categoria"><option value="fabricacion" ${obj && obj.categoria === 'fabricacion' ? 'selected' : ''}>Fabricación</option><option value="reventa" ${obj && obj.categoria === 'reventa' ? 'selected' : ''}>Reventa</option></select></div><div class="form-group"><label>Clasificación</label><select name="clasificacion"><option value="Unicolor" ${clasif==='Unicolor'?'selected':''}>Unicolor</option><option value="Combinada" ${clasif==='Combinada'?'selected':''}>Combinada</option><option value="Especial" ${clasif==='Especial'?'selected':''}>Especial</option><option value="Reventa" ${clasif==='Reventa'?'selected':''}>Reventa</option><option value="Otro" ${clasif==='Otro'?'selected':''}>Otro</option></select></div></div><div class="grid-2"><div class="form-group"><label>Precio Venta ($)</label><input type="number" name="precio_venta" value="${obj ? obj.precio_venta : ''}" required></div><div class="form-group"><label>Precio Mayoreo ($)</label><input type="number" name="precio_mayoreo" value="${obj ? (obj.precio_mayoreo||'') : ''}"></div></div><div style="background: #FFFBEB; padding: 10px; border-radius: 8px; margin-bottom: 15px;"><strong style="color: #D69E2E; font-size: 0.9rem;">📦 Receta de Inventario</strong><br><small style="color: var(--text-muted);">Puedes descontar hasta 5 insumos al vender.</small><div style="margin-top:10px;">${recetaHTML}</div></div><button type="submit" class="btn btn-primary" style="width: 100%;">${obj ? 'Guardar Cambios' : 'Crear Producto'}</button></form>`; App.ui.openSheet(obj ? "Editar Producto" : "Nuevo Producto", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("productos", id, data, "productos"); else App.logic.guardarNuevoGenerico("productos", data, "PROD", "productos"); }); },
        formTarifa(artesanoId) { const formHTML = `<form id="dynamic-form"><input type="hidden" name="artesano_id" value="${artesanoId}"><div class="form-group"><label>Tarea a Realizar (Ej. Tejer Cuerpo)</label><input type="text" name="clasificacion" required placeholder="Nombre de la tarea"></div><div class="form-group"><label>Monto a Pagar ($)</label><input type="number" step="0.01" name="monto" required></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px;">Guardar Tarea</button></form>`; App.ui.openSheet("Nueva Tarea para Artesano", formHTML, (data) => App.logic.guardarNuevoGenerico("tarifas_artesano", data, "TAR", "tarifas_artesano")); },
        formEditarPedido(id) { const p = App.state.pedidos.find(x => x.id === id); const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Precio Total ($)</label><input type="number" step="0.01" name="total" value="${p.total}" required></div><div class="form-group"><label>Anticipo Registrado ($)</label><input type="number" step="0.01" name="anticipo" value="${p.anticipo}" required></div><div class="form-group"><label>Fecha de Entrega</label><input type="date" name="fecha_entrega" value="${p.fecha_entrega || ''}"></div><div class="form-group"><label>Notas / Instrucciones</label><textarea name="notas" rows="2">${p.notas || ''}</textarea></div><button type="submit" class="btn btn-primary" style="width: 100%;">Guardar Cambios</button></form>`; App.ui.openSheet("Editar Pedido", formHTML, (data) => { App.logic.actualizarRegistroGenerico("pedidos", id, data, "pedidos"); }); },
        formCliente(id = null) { const obj = id ? App.state.clientes.find(c => c.id === id) : null; const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Nombre</label><input type="text" name="nombre" value="${obj ? obj.nombre : ''}" required></div><div class="form-group"><label>Teléfono</label><input type="tel" name="telefono" value="${obj ? obj.telefono : ''}"></div><div class="form-group"><label>Dirección</label><input type="text" name="direccion" value="${obj ? obj.direccion : ''}"></div><button type="submit" class="btn btn-primary" style="width: 100%;">${obj ? 'Guardar Cambios' : 'Crear Cliente'}</button></form>`; App.ui.openSheet(obj ? "Editar Cliente" : "Nuevo Cliente", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("clientes", id, data, "clientes"); else App.logic.guardarNuevoGenerico("clientes", data, "CLI", "clientes"); }); },
        formProveedor(id = null) { const obj = id ? App.state.proveedores.find(p => p.id === id) : null; const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Nombre del Proveedor</label><input type="text" name="nombre" value="${obj ? obj.nombre : ''}" required></div><div class="form-group"><label>Teléfono</label><input type="tel" name="telefono" value="${obj ? obj.telefono : ''}"></div><button type="submit" class="btn btn-primary" style="width: 100%;">${obj ? 'Guardar Cambios' : 'Crear Proveedor'}</button></form>`; App.ui.openSheet(obj ? "Editar Proveedor" : "Nuevo Proveedor", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("proveedores", id, data, "proveedores"); else App.logic.guardarNuevoGenerico("proveedores", data, "PRV", "proveedores"); }); },
        formArtesano(id = null) { const obj = id ? App.state.artesanos.find(a => a.id === id) : null; const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Nombre del Artesano</label><input type="text" name="nombre" value="${obj ? obj.nombre : ''}" required></div><div class="form-group"><label>Teléfono</label><input type="tel" name="telefono" value="${obj ? obj.telefono : ''}"></div><button type="submit" class="btn btn-primary" style="width: 100%;">${obj ? 'Guardar Cambios' : 'Crear Artesano'}</button></form>`; App.ui.openSheet(obj ? "Editar Artesano" : "Nuevo Artesano", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("artesanos", id, data, "artesanos"); else App.logic.guardarNuevoGenerico("artesanos", data, "ART", "artesanos"); }); },
        formGasto(id = null) { const obj = id ? App.state.gastos.find(g => g.id === id) : null; const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Descripción del Gasto</label><input type="text" name="descripcion" value="${obj ? obj.descripcion : ''}" required></div><div class="form-group"><label>Categoría</label><select name="categoria" required><option value="materiales" ${obj && obj.categoria === 'materiales' ? 'selected' : ''}>Materiales</option><option value="servicios" ${obj && obj.categoria === 'servicios' ? 'selected' : ''}>Servicios</option><option value="nomina" ${obj && obj.categoria === 'nomina' ? 'selected' : ''}>Nómina</option><option value="otro" ${obj && obj.categoria === 'otro' ? 'selected' : ''}>Otro</option></select></div><div class="form-group"><label>Monto ($)</label><input type="number" step="0.01" name="monto" value="${obj ? obj.monto : ''}" required></div><div class="form-group"><label>Fecha</label><input type="date" name="fecha" value="${obj ? obj.fecha : new Date().toISOString().split('T')[0]}" required></div><button type="submit" class="btn btn-primary" style="width: 100%; background: var(--danger); border-color: var(--danger);">${obj ? 'Guardar Cambios' : 'Registrar Gasto'}</button></form>`; App.ui.openSheet(obj ? "Editar Gasto" : "Nuevo Gasto", formHTML, (data) => { if (obj) App.logic.actualizarRegistroGenerico("gastos", id, data, "gastos"); else App.logic.guardarNuevoGenerico("gastos", data, "GAS", "gastos"); }); },
        formNuevoPedido() { const opcionesClientes = App.state.clientes.map(c => `<option value="${c.id}">${c.nombre}</option>`).join(''); const opcionesProductos = App.state.productos.map(p => `<option value="${p.id}">${p.nombre}</option>`).join(''); const formHTML = `<form id="dynamic-form"><div style="background: #EBF8FF; padding: 8px; border-radius: 6px; margin-bottom: 10px; font-size: 0.8rem;"><strong>Aviso:</strong> El inventario se descontará automáticamente.</div><div class="form-group"><label>Cliente</label><select name="cliente_id" required><option value="">-- Elige un cliente --</option>${opcionesClientes}</select></div><div class="form-group"><label>Producto</label><select name="producto_id" required onchange="window.calcularTotalPedido()"><option value="">-- Elige un producto --</option>${opcionesProductos}</select></div><div class="form-group"><label style="display:flex; align-items:center; gap:8px;"><input type="checkbox" name="es_mayoreo" value="1" onchange="window.calcularTotalPedido()" style="width:20px; height:20px;"><strong>Aplicar Precio Mayoreo</strong></label></div><div class="form-group"><label>Cantidad</label><input type="number" name="cantidad" value="1" min="1" required oninput="window.calcularTotalPedido()"></div><div class="form-group"><label>Precio Total ($)</label><input type="number" name="total" required></div><div class="form-group"><label>Anticipo ($)</label><input type="number" name="anticipo" value="0" required></div><div class="form-group"><label>Fecha Entrega Estimada</label><input type="date" name="fecha_entrega" required></div><div class="form-group"><label>Notas</label><input type="text" name="notas" placeholder="Opcional"></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px;">Crear Pedido</button></form>`; App.ui.openSheet("Nuevo Pedido", formHTML, (data) => App.logic.guardarNuevoPedido(data)); },
        formOrdenStock() { const opcionesProductos = App.state.productos.map(p => `<option value="${p.id}">${p.nombre}</option>`).join(''); const formHTML = `<form id="dynamic-form"><div style="background: #FEFCBF; padding: 10px; border-radius: 8px; margin-bottom: 15px; font-size: 0.85rem;"><strong>Fabricar para Bodega</strong><br>Descontará hilos y enviará el trabajo a artesanos, sin requerir cliente.</div><div class="form-group"><label>¿Qué vas a fabricar?</label><select name="producto_id" required><option value="">-- Elige un producto --</option>${opcionesProductos}</select></div><div class="form-group"><label>Cantidad a fabricar</label><input type="number" name="cantidad" value="1" min="1" required></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px;">Mandar a Producción</button></form>`; App.ui.openSheet("Producir para Stock", formHTML, (data) => App.logic.guardarOrdenStock(data)); },
        formCobrar(pedidoId, clienteId, saldoPendiente) { const formHTML = `<form id="dynamic-form"><input type="hidden" name="pedido_id" value="${pedidoId}"><input type="hidden" name="cliente_id" value="${clienteId}"><div class="form-group"><label>Abonar ($)</label><input type="number" name="monto" value="${saldoPendiente}" max="${saldoPendiente}" required></div><div class="form-group"><label>Nota/Método</label><input type="text" name="nota" value="Efectivo" required></div><button type="submit" class="btn btn-primary" style="width: 100%;">Confirmar Pago</button></form>`; App.ui.openSheet(`Pago ${pedidoId}`, formHTML, (data) => App.logic.guardarAbono(data)); },
        formCompra() { const opcProv = App.state.proveedores.map(p => `<option value="${p.id}">${p.nombre}</option>`).join(''); const opcMat = App.state.inventario.map(m => `<option value="${m.id}">${m.nombre} (Stock: ${m.stock_actual})</option>`).join(''); let itemsHTML = ''; for(let i=1; i<=5; i++) { itemsHTML += `<div class="grid-2" style="margin-bottom: 5px;"><div class="form-group" style="margin:0;"><label>Insumo ${i}</label><select name="mat_${i}"><option value="">-- Ninguno --</option>${opcMat}</select></div><div class="form-group" style="margin:0;"><label>Cantidad</label><input type="number" step="0.1" name="cant_${i}"></div></div>`; } const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Proveedor</label><select name="proveedor_id" required><option value="">-- Elige Proveedor --</option>${opcProv}</select></div><div style="background: #EBF8FF; padding: 10px; border-radius: 8px; margin-bottom: 15px;"><strong style="font-size: 0.9rem; color: var(--primary);">📥 Artículos Comprados</strong><br><span style="color:var(--primary); cursor:pointer; font-weight:bold; font-size:0.8rem; display:block; margin-bottom:10px; padding:5px; border:1px dashed var(--primary); text-align:center;" onclick="App.ui.closeSheet(); setTimeout(()=>App.views.formMaterial(), 400);">+ No encuentro mi insumo (Crear Nuevo)</span><div>${itemsHTML}</div></div><div class="form-group"><label>Fecha</label><input type="date" name="fecha" value="${new Date().toISOString().split('T')[0]}" required></div><div class="form-group"><label>Costo Total Factura ($)</label><input type="number" name="total" required></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px; background: var(--success); border-color: var(--success);">Confirmar Compra</button></form>`; App.ui.openSheet("Ingresar Compra Múltiple", formHTML, (data) => App.logic.guardarNuevaCompra(data)); },
        formEditarCompra(id) { const obj = App.state.compras.find(c => c.id === id); const opcProv = App.state.proveedores.map(p => `<option value="${p.id}" ${obj.proveedor_id === p.id ? 'selected':''}>${p.nombre}</option>`).join(''); const formHTML = `<form id="dynamic-form"><div style="background: #FEFCBF; padding: 10px; border-radius: 8px; margin-bottom: 15px; font-size: 0.85rem;"><strong>Nota:</strong> Solo puedes cambiar Proveedor, Fecha y Total. Para cambiar cantidades, ELIMINA la compra y vuelve a hacerla.</div><div class="form-group"><label>Proveedor</label><select name="proveedor_id" required><option value="">-- Elige Proveedor --</option>${opcProv}</select></div><div class="form-group"><label>Fecha</label><input type="date" name="fecha" value="${obj.fecha}" required></div><div class="form-group"><label>Costo Total Pagado ($)</label><input type="number" name="total" value="${obj.total}" required></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px;">Guardar Cambios</button></form>`; App.ui.openSheet("Editar Compra", formHTML, (data) => App.logic.actualizarRegistroGenerico("compras", id, data, "compras")); },
        modalEditarOrden(ordenId) { const orden = App.state.ordenes_produccion.find(o => o.id === ordenId); const pagos = App.state.pago_artesanos.filter(p => p.orden_id === ordenId); const costoManoObra = pagos.reduce((sum, p) => sum + parseFloat(p.total), 0); let pagosHTML = '<div style="margin-bottom:15px;"><strong>🛠️ Trabajos Asignados a esta Hamaca:</strong><br>'; if(pagos.length === 0) pagosHTML += '<small style="color:var(--text-muted)">Nadie ha trabajado en esta orden aún.</small>'; pagos.forEach(p => { const art = App.state.artesanos.find(a => a.id === p.artesano_id); const partesID = p.id.split('-'); const nombreTarea = partesID.length > 2 ? partesID.slice(2).join(' ') : 'Trabajo general'; pagosHTML += `<div style="display:flex; justify-content:space-between; background:#F7FAFC; padding:8px; margin-top:5px; border-radius:4px; font-size:0.85rem; border:1px solid #E2E8F0;"><span>🧑‍🎨 <strong>${art ? art.nombre : 'Desc'}</strong> - <em>${nombreTarea}</em></span><span><strong style="color:var(--success); margin-right:8px;">$${p.total}</strong> <span style="cursor:pointer; color:red;" onclick="App.logic.eliminarRegistroGenerico('pago_artesanos', '${p.id}', 'pago_artesanos'); App.ui.closeSheet();">🗑️</span></span></div>`; }); pagosHTML += `<div style="margin-top: 10px; text-align: right; font-weight: bold; color: var(--primary);">Costo Mano de Obra: $${costoManoObra.toFixed(2)}</div></div><button type="button" class="btn btn-secondary" style="width:100%; margin-bottom:15px; border-style:dashed;" onclick="App.ui.closeSheet(); setTimeout(()=>App.views.formAgregarTrabajo('${ordenId}', false), 300);">+ Asignar Tarea / Artesano</button>`; const formHTML = `<form id="dynamic-form">${pagosHTML}<div class="form-group"><label>Estado del Pedido en Taller</label><select name="estado"><option value="pendiente" ${orden.estado === 'pendiente' ? 'selected' : ''}>Pendiente de tejer</option><option value="en_proceso" ${orden.estado === 'en_proceso' ? 'selected' : ''}>En Proceso (Tejiendo)</option><option value="listo" ${orden.estado === 'listo' ? 'selected' : ''}>¡Terminada! (Pasar a cierre)</option></select></div><button type="submit" class="btn btn-primary" style="width: 100%;">Actualizar Estado</button></form>`; App.ui.openSheet(`Orden ${ordenId}`, formHTML, (datos) => App.logic.procesarCambioOrden(ordenId, datos)); },
        formAgregarTrabajo(ordenId, esReparacion = false) { const opcArt = App.state.artesanos.map(a => `<option value="${a.id}">${a.nombre}</option>`).join(''); const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Artesano</label><select name="artesano_id" required onchange="window.cargarTarifas(this.value)"><option value="">-- Selecciona --</option>${opcArt}</select></div><div class="form-group"><label>Tarea a Realizar</label><select name="tarea_val" id="select-tarifas" required onchange="window.calcTotalTrabajo()"><option value="">-- Elige un artesano primero --</option></select></div><input type="hidden" name="tarea_nombre" id="tarea_nombre"><div class="form-group"><label>Cantidad (Ej. 7 hilos, o 1 pza)</label><input type="number" step="0.1" name="cantidad" id="cant-trabajo" value="1" required oninput="window.calcTotalTrabajo()"></div><div class="form-group"><label>Total a Pagar ($)</label><input type="number" step="0.01" name="total" id="total-trabajo" required readonly style="background:#f0f0f0;"></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 10px;">Guardar Trabajo</button></form>`; App.ui.openSheet("Añadir Tarea", formHTML, (data) => App.logic.guardarTrabajoOrden(ordenId, data, esReparacion)); },
        formCerrarOrden(ordenId) { const orden = App.state.ordenes_produccion.find(o => o.id === ordenId); const detalle = App.state.pedido_detalle.find(d => d.pedido_id === orden.pedido_detalle_id); const producto = detalle ? App.state.productos.find(p => p.id === detalle.producto_id) : { nombre: 'Hamaca', tubos_hilo: 0 }; let materialesHTML = ''; for(let i=1; i<=5; i++){ const matId = producto[`mat_${i}`]; const cant = parseFloat(producto[`cant_${i}`]); const uso = producto[`uso_${i}`] || 'General'; if(matId && cant > 0) { const material = App.state.inventario.find(m => m.id === matId); const consumoTeorico = cant * parseInt(detalle ? detalle.cantidad : 1); materialesHTML += `<div class="form-group" style="margin-bottom:8px; background: white; padding: 8px; border-radius: 4px; border: 1px solid #e2e8f0;"><label>${material ? material.nombre : 'Insumo'} <strong>[${uso.toUpperCase()}]</strong></label><input type="hidden" name="mat_${i}_id" value="${matId}"><input type="hidden" name="mat_${i}_teorico" value="${consumoTeorico}"><div style="display:flex; align-items:center; gap: 10px;"><span style="font-size:0.8rem;">Consumo Real:</span><input type="number" step="0.1" name="mat_${i}_real" value="${consumoTeorico}" required style="flex:1;"></div></div>`; } } if(materialesHTML === '') materialesHTML = '<p style="color:var(--text-muted); font-size:0.8rem;">No hay insumos a descontar para este producto.</p>'; const formHTML = `<div style="background: #FEFCBF; padding: 10px; border-radius: 8px; margin-bottom: 15px; font-size: 0.9rem;"><strong>Cierre de Producción</strong><br>Producto: ${producto.nombre}</div><form id="dynamic-form"><input type="hidden" name="orden_id" value="${ordenId}"><p style="font-size:0.8rem; color:var(--text-muted); margin-bottom:10px;">Si hubo merma y usaron más hilo del apartado, ajústalo aquí.</p>${materialesHTML}<div style="margin-top: 15px; padding: 10px; background: #E2E8F0; border-radius: 6px;"><label style="display:flex; align-items:center; gap:8px;"><input type="checkbox" name="sumar_stock" value="1" style="width:20px; height:20px;"><strong>Guardar en mi Bodega Física</strong></label><small style="color:var(--text-muted); display:block; margin-top:5px;">Si esta hamaca NO se va a entregar a un cliente hoy, marca esta casilla para guardarla en tu inventario de reventa.</small></div><button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 15px; background: var(--success); border-color: var(--success);">Finalizar Orden</button></form>`; App.ui.openSheet("Cerrar Orden", formHTML, (datos) => App.logic.cerrarOrdenProduccion(datos)); },
        formNuevaReparacion() { const opcionesClientes = App.state.clientes.map(c => `<option value="${c.id}">${c.nombre}</option>`).join(''); const formHTML = `<form id="dynamic-form"><div class="form-group"><label>Cliente</label><select name="cliente_id" required><option value="">-- Elige un cliente --</option>${opcionesClientes}</select></div><div class="form-group"><label>Descripción</label><input type="text" name="descripcion" required placeholder="Ej. Cambio de brazos"></div><div class="form-group"><label>Precio ($)</label><input type="number" name="precio" required></div><div class="form-group"><label>Anticipo ($)</label><input type="number" name="anticipo" value="0" required></div><button type="submit" class="btn btn-primary" style="width: 100%;">Registrar Reparación</button></form>`; App.ui.openSheet("Nueva Reparación", formHTML, (data) => App.logic.guardarNuevaReparacion(data)); }
    }
};

App.router = { 
    init() { window.addEventListener('hashchange', () => this.handleRoute()); this.handleRoute(); }, 
    navigate(route) { window.location.hash = route; }, 
    handleRoute() { 
        if (!App.state.pinAcceso) { App.ui.hideLoader(); document.getElementById('app-content').innerHTML = App.views.login(); document.getElementById('header-title').textContent = "Acceso Restringido"; return; }
        let hash = window.location.hash.substring(1) || 'inicio'; const contentDiv = document.getElementById('app-content'); const titleEl = document.getElementById('header-title'); 
        document.querySelectorAll('.nav-item').forEach(el => el.classList.remove('active')); const activeNav = document.querySelector(`.nav-item[data-view="${hash}"]`); if(activeNav) activeNav.classList.add('active'); 
        if (App.views[hash]) { contentDiv.innerHTML = App.views[hash](); titleEl.textContent = hash.charAt(0).toUpperCase() + hash.slice(1); } 
        else { contentDiv.innerHTML = `<div class="card"><p>Módulo no encontrado.</p></div>`; } 
    } 
};

App.start = function() { this.ui.init(); if (!this.state.pinAcceso) { this.router.init(); } else { this.logic.cargarDatosIniciales(); } };
document.addEventListener('DOMContentLoaded', () => App.start());
